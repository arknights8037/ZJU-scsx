<template>
  <!-- 根路由出口：根据 URL 路径渲染对应的页面组件（portal 门户端或 admin 管理后台） -->
  <router-view />
</template>

<script setup>
/**
 * App - 应用根组件
 *
 * 功能：
 * - 作为 Vue 应用的入口组件
 * - 通过 <router-view> 渲染当前路由对应的页面
 * - 不包含任何业务逻辑，仅作为路由出口容器
 */
</script>
